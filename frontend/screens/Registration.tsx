import { SafeAreaView } from "react-native";
import Form from "../components/Form";
const Registration = () => {
  return (
    <SafeAreaView>
      <Form name={"loginForm"}/>
    </SafeAreaView>
  );
};
export default Registration;
